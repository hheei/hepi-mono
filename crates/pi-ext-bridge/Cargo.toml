[package]
name = "pi-ext-bridge"
version = "0.1.0"
edition = "2024"
license = "MIT"
publish = false

[lib]
crate-type = ["cdylib"]

[dependencies]
mpatch = { path = "../vendor/mpatch" }
libc = { workspace = true }
napi = { workspace = true }
napi-derive = "3"
portable-pty = { workspace = true }
tokio = { workspace = true }

[build-dependencies]
napi-build = "2"
