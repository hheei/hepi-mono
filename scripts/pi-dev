#!/usr/bin/env sh
set -eu

script_dir=$(CDPATH= cd -- "$(dirname -- "$0")" && pwd)
root=$(CDPATH= cd -- "$script_dir/.." && pwd)

exec node \
	--no-warnings \
	--import "$root/node_modules/jiti/lib/jiti-register.mjs" \
	"$root/scripts/pi-dev.ts" \
	"$@"
