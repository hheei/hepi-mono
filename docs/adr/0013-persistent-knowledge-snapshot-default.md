# Persistent knowledge snapshot default

MCTX knowledge snapshots default to `persistent` so validated Hindsight-derived context can survive session resume and handoff. First use discloses the local copy range; `ephemeral` and `disabled` remain explicit privacy choices. This accepts a local derived-text copy in exchange for deterministic continuity.
