# Pi Subagents

Task-only child-agent extension for Pi. Requires Pi `>=0.82.0` and `@hheei/pi-ext-core`.

Install as a Pi package. The extension exposes the model-facing `agent` tool and delivers terminal results as follow-up messages.
