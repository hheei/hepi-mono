# @hheei/pi-t2s

Converts interactive Traditional Chinese prose to Simplified Chinese while preserving inline and fenced code.

Install with:

```bash
pi install npm:@hheei/pi-t2s
```

Install `@hheei/pi-settings` to edit its mode. Saved changes apply after `/reload` or a new session. Requires Pi `>=0.83.0`.
