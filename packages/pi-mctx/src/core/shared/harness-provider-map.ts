/** Pi configuration stores native `provider/model` references. */
export function piModelRefToCanonical(ref: string): string {
    return ref;
}

export function resolveModelRefForPi(ref: string): string {
    return ref;
}
