# @hheei/pi-btw

Pi extension for asking read-only side questions about the current session.

Install this package in Pi. It registers the `/btw` command and depends on
`@hheei/pi-ext-core` for shared extension lifecycle coordination.
