# @hheei/pi-settings

Settings-surface host for HEPI Pi extensions. Install it with `@hheei/pi-ext-core` and settings-provider packages such as `@hheei/pi-ext-tools`.

Run `/ext-settings` in Pi TUI mode. The package hosts registered settings pages and temporarily suspends core-managed editor widgets while the surface is open.
