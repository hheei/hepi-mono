# @hheei/pi-auto-title

Independent Pi extension that generates concise session titles after the first settled turn.

It owns title policy, settings fields, input clipping, validation, status shimmer, and retry timing. Subagent
completion lifecycle and atomic settings storage come from `@hheei/pi-ext-core`.
