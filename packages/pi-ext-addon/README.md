# @hheei/pi-ext-addon

Pi host addon for OpenAI Responses replay compatibility.

## Install

Install through Pi's package manager or add `@hheei/pi-ext-addon` to the Pi package configuration. The extension is published at `dist/extension.js`.
