diff --git a/dist/index.d.ts b/dist/index.d.ts
index 919db7d16bba002ca3cfb7c87f1a6800eb296533..a69cd782b71266d16c1a1a7767d4efaf66a9ee7d 100644
--- a/dist/index.d.ts
+++ b/dist/index.d.ts
@@ -19,6 +19,6 @@ export { StdinBuffer, type StdinBufferEventMap, type StdinBufferOptions } from "
 export { ProcessTerminal, type Terminal } from "./terminal.ts";
 export { parseOsc11BackgroundColor, parseTerminalColorSchemeReport, type RgbColor, type TerminalColorScheme, } from "./terminal-colors.ts";
 export { allocateImageId, type CellDimensions, calculateImageRows, deleteAllKittyImages, deleteKittyImage, detectCapabilities, encodeITerm2, encodeKitty, getCapabilities, getCellDimensions, getGifDimensions, getImageDimensions, getJpegDimensions, getPngDimensions, getWebpDimensions, hyperlink, type ImageDimensions, type ImageProtocol, type ImageRenderOptions, imageFallback, renderImage, resetCapabilitiesCache, setCapabilities, setCellDimensions, type TerminalCapabilities, } from "./terminal-image.ts";
-export { type Component, Container, CURSOR_MARKER, type Focusable, isFocusable, type OverlayAnchor, type OverlayHandle, type OverlayMargin, type OverlayOptions, type OverlayUnfocusOptions, type SizeValue, TUI, } from "./tui.ts";
+export { type Component, type ComponentBounds, Container, CURSOR_MARKER, type Focusable, isFocusable, type OverlayAnchor, type OverlayHandle, type OverlayMargin, type OverlayOptions, type OverlayUnfocusOptions, type SizeValue, TUI, } from "./tui.ts";
 export { sliceByColumn, truncateToWidth, visibleWidth, wrapTextWithAnsi } from "./utils.ts";
 //# sourceMappingURL=index.d.ts.map
\ No newline at end of file
diff --git a/dist/tui.d.ts b/dist/tui.d.ts
index 85c381ed471a37d3dbf072705320779a67085b54..6bb74246456b0f99267768f87dcb207d7d8d35bc 100644
--- a/dist/tui.d.ts
+++ b/dist/tui.d.ts
@@ -29,6 +29,12 @@ export interface Component {
      */
     invalidate(): void;
 }
+export interface ComponentBounds {
+    readonly x: number;
+    readonly y: number;
+    readonly width: number;
+    readonly height: number;
+}
 type InputListenerResult = {
     consume?: boolean;
     data?: string;
@@ -210,6 +216,8 @@ export declare class TUI extends Container {
     private queryCellSize;
     stop(): void;
     requestRender(force?: boolean): void;
+    getComponentBounds(component: Component): ComponentBounds | undefined;
+    onComponentLayout(component: Component, listener: (bounds: ComponentBounds) => void): () => void;
     private scheduleRender;
     private handleInput;
     private consumeOsc11BackgroundResponse;
diff --git a/dist/tui.js b/dist/tui.js
index 7e6b41a8d95d7707d0e2d160dc64cd4b57fbf37f..c92ff10ec2ec374239ea294056a5f512adf30ffa 100644
--- a/dist/tui.js
+++ b/dist/tui.js
@@ -54,6 +54,19 @@ export function isFocusable(component) {
  * TUI finds and strips this marker, then positions the hardware cursor there.
  */
 export const CURSOR_MARKER = "\x1b_pi:c\x07";
+let activeLayoutCollector;
+function renderWithLayout(component, width, x, y) {
+    const collector = activeLayoutCollector;
+    const previousOrigin = collector?.origin;
+    if (collector)
+        collector.origin = { x, y };
+    const lines = component.render(width);
+    if (collector) {
+        collector.bounds.set(component, { x, y, width, height: lines.length });
+        collector.origin = previousOrigin;
+    }
+    return lines;
+}
 export { visibleWidth };
 /** Parse a SizeValue into absolute value given a reference size */
 function parseSizeValue(value, referenceSize) {
@@ -95,8 +108,9 @@ export class Container {
     }
     render(width) {
         const lines = [];
+        const origin = activeLayoutCollector?.origin ?? { x: 0, y: 0 };
         for (const child of this.children) {
-            const childLines = child.render(width);
+            const childLines = renderWithLayout(child, width, origin.x, origin.y + lines.length);
             for (const line of childLines) {
                 lines.push(line);
             }
@@ -138,6 +152,8 @@ export class TUI extends Container {
     focusOrderCounter = 0;
     overlayStack = [];
     overlayFocusRestore = { status: "inactive" };
+    componentBounds = new WeakMap();
+    componentLayoutListeners = new Map();
     constructor(terminal, showHardwareCursor, logDirectory) {
         super();
         this.terminal = terminal;
@@ -528,6 +544,25 @@ export class TUI extends Container {
         this.renderRequested = true;
         process.nextTick(() => this.scheduleRender());
     }
+    getComponentBounds(component) {
+        return this.componentBounds.get(component);
+    }
+    onComponentLayout(component, listener) {
+        let listeners = this.componentLayoutListeners.get(component);
+        if (!listeners) {
+            listeners = new Set();
+            this.componentLayoutListeners.set(component, listeners);
+        }
+        listeners.add(listener);
+        const bounds = this.componentBounds.get(component);
+        if (bounds)
+            listener(bounds);
+        return () => {
+            listeners?.delete(listener);
+            if (listeners?.size === 0)
+                this.componentLayoutListeners.delete(component);
+        };
+    }
     scheduleRender() {
         if (this.stopped || this.renderTimer || !this.renderRequested) {
             return;
@@ -994,7 +1029,23 @@ export class TUI extends Container {
             return targetScreenRow - currentScreenRow;
         };
         // Render all components to get new lines
-        let newLines = this.render(width);
+        activeLayoutCollector = { origin: { x: 0, y: 0 }, bounds: new Map() };
+        let newLines;
+        let collector;
+        try {
+            newLines = this.render(width);
+        }
+        finally {
+            collector = activeLayoutCollector;
+            activeLayoutCollector = undefined;
+        }
+        const viewportTopForLayout = Math.max(0, newLines.length - height);
+        for (const [component, bounds] of collector.bounds) {
+            const viewportBounds = { ...bounds, y: bounds.y - viewportTopForLayout };
+            this.componentBounds.set(component, viewportBounds);
+            for (const listener of this.componentLayoutListeners.get(component) ?? [])
+                listener(viewportBounds);
+        }
         // Composite overlays into the rendered lines (before differential compare)
         if (this.overlayStack.length > 0) {
             newLines = this.compositeOverlays(newLines, width, height);
