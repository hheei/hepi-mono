diff --git a/dist/components/editor.js b/dist/components/editor.js
index 0000000..0000001 100644
--- a/dist/components/editor.js
+++ b/dist/components/editor.js
@@ -613,6 +613,7 @@ export class Editor {
             if (kb.matches(data, "tui.input.tab")) {
                 const selected = this.autocompleteList.getSelectedItem();
                 if (selected && this.autocompleteProvider) {
+                    const shouldRefreshSlashCompletion = this.autocompletePrefix.startsWith("/") && !this.autocompletePrefix.includes(" ");
                     this.pushUndoSnapshot();
                     this.lastAction = null;
                     const result = this.autocompleteProvider.applyCompletion(this.state.lines, this.state.cursorLine, this.state.cursorCol, selected, this.autocompletePrefix);
@@ -622,6 +623,8 @@ export class Editor {
                     this.cancelAutocomplete();
                     if (this.onChange)
                         this.onChange(this.getText());
+                    if (shouldRefreshSlashCompletion)
+                        this.tryTriggerAutocomplete();
                 }
                 return;
             }
diff --git a/dist/tui-main-screen.js b/dist/tui-main-screen.js
index 0000000..0000001 100644
--- a/dist/tui-main-screen.js
+++ b/dist/tui-main-screen.js
@@ -241,13 +241,16 @@ export class TuiMainScreen extends TuiBase {
         const cursorPos = this.extractCursorPosition(newLines, height);
         newLines = this.applyLineResets(newLines);
         // Helper to clear scrollback and viewport and render all new lines
-        const fullRender = (clear) => {
+        const fullRender = (clear, clearScrollback = clear) => {
             this.fullRedrawCount += 1;
             const output = new BoundedTerminalWriter((data) => this.terminal.write(data));
             output.append("\x1b[?2026h"); // Begin synchronized output
             if (clear) {
                 output.append(this.deleteKittyImages(this.previousKittyImageIds));
-                output.append("\x1b[2J\x1b[H\x1b[3J"); // Clear screen, home, then clear scrollback
+                output.append("\x1b[2J\x1b[H"); // Clear screen and home
+                if (clearScrollback) {
+                    output.append("\x1b[3J"); // Clear scrollback
+                }
             }
             for (let i = 0; i < newLines.length; i++) {
                 if (i > 0)
@@ -304,7 +307,9 @@ export class TuiMainScreen extends TuiBase {
         // Width changes always need a full re-render because wrapping changes.
         if (widthChanged) {
             logRedraw(`terminal width changed (${this.previousWidth} -> ${width})`);
-            fullRender(true);
+            // A streaming update can change a tool row that is above the visible viewport.
+            // Repaint it without deleting the user's terminal scrollback.
+            fullRender(true, false);
             return;
         }
         // Height changes normally need a full re-render to keep the visible viewport aligned,
