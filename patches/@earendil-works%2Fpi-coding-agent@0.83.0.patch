diff --git a/dist/core/extensions/loader.js b/dist/core/extensions/loader.js
index 099e74e87220ed9c57990771d0466416184d4304..6a852cd0f18552d2e2925823a53af4991378343f 100644
--- a/dist/core/extensions/loader.js
+++ b/dist/core/extensions/loader.js
@@ -223,6 +223,10 @@ function createExtensionAPI(extension, runtime, cwd, eventBus) {
             runtime.assertActive();
             extension.messageRenderers.set(customType, renderer);
         },
+        registerAssistantMessageAddon(factory) {
+            runtime.assertActive();
+            extension.assistantMessageAddon = factory;
+        },
         registerEntryRenderer(customType, renderer) {
             runtime.assertActive();
             extension.entryRenderers ??= new Map();
@@ -354,6 +358,7 @@ function createExtension(extensionPath, resolvedPath) {
         handlers: new Map(),
         tools: new Map(),
         messageRenderers: new Map(),
+        assistantMessageAddon: undefined,
         entryRenderers: new Map(),
         commands: new Map(),
         flags: new Map(),
diff --git a/dist/core/extensions/runner.d.ts b/dist/core/extensions/runner.d.ts
index 99b17f67a68ce42eaac25816aae206bb544ea02e..8cd2d51184eeb5e89ad914ed82e56dd0030c7d94 100644
--- a/dist/core/extensions/runner.d.ts
+++ b/dist/core/extensions/runner.d.ts
@@ -9,6 +9,7 @@ import type { KeybindingsConfig } from "../keybindings.ts";
 import type { ModelRegistry } from "../model-registry.ts";
 import type { SessionManager } from "../session-manager.ts";
 import type { BuildSystemPromptOptions } from "../system-prompt.ts";
+import type { AssistantMessageAddonFactory } from "./types.ts";
 import type { BeforeAgentStartEvent, BeforeAgentStartEventResult, BeforeProviderHeadersEvent, BeforeProviderRequestEvent, ContextEvent, EntryRenderer, Extension, ExtensionActions, ExtensionCommandContext, ExtensionCommandContextActions, ExtensionContext, ExtensionContextActions, ExtensionError, ExtensionEvent, ExtensionFlag, ExtensionMode, ExtensionRuntime, ExtensionShortcut, ExtensionUIContext, InputEvent, InputEventResult, InputSource, LoadExtensionsResult, MessageEndEvent, MessageRenderer, ProjectTrustContext, ProjectTrustEvent, ProjectTrustEventResult, ProviderConfig, RegisteredTool, ReplacedSessionContext, ResolvedCommand, ResourcesDiscoverEvent, SessionBeforeCompactResult, SessionBeforeForkResult, SessionBeforeSwitchResult, SessionBeforeTreeResult, SessionShutdownEvent, ToolCallEvent, ToolCallEventResult, ToolResultEvent, ToolResultEventResult, UserBashEvent, UserBashEventResult } from "./types.ts";
 /** Combined result from all before_agent_start handlers */
 interface BeforeAgentStartCombinedResult {
@@ -123,6 +124,7 @@ export declare class ExtensionRunner {
     emitError(error: ExtensionError): void;
     hasHandlers(eventType: string): boolean;
     getMessageRenderer(customType: string): MessageRenderer | undefined;
+    getAssistantMessageAddonFactory(): AssistantMessageAddonFactory | undefined;
     getEntryRenderer(customType: string): EntryRenderer | undefined;
     private resolveRegisteredCommands;
     getModelRegistry(): ModelRegistry;
diff --git a/dist/core/extensions/runner.js b/dist/core/extensions/runner.js
index 265b2fa147e215080ef1fedd22c44a6199f48c23..e559d142cf89e3ccb2c9ad2c724d04ae54497359 100644
--- a/dist/core/extensions/runner.js
+++ b/dist/core/extensions/runner.js
@@ -387,6 +387,13 @@ export class ExtensionRunner {
         }
         return undefined;
     }
+    getAssistantMessageAddonFactory() {
+        for (const ext of this.extensions) {
+            if (ext.assistantMessageAddon)
+                return ext.assistantMessageAddon;
+        }
+        return undefined;
+    }
     getEntryRenderer(customType) {
         for (const ext of this.extensions) {
             const renderer = ext.entryRenderers?.get(customType);
diff --git a/dist/core/extensions/types.d.ts b/dist/core/extensions/types.d.ts
index 1a06f40b215da8bba3abfb76d28f9cc8ce17d3e7..8db919e95b4e9f8a70c80f4e7b2dc88b8c68b958 100644
--- a/dist/core/extensions/types.d.ts
+++ b/dist/core/extensions/types.d.ts
@@ -9,7 +9,7 @@
  */
 import type { AgentMessage, AgentToolResult, AgentToolUpdateCallback, ThinkingLevel, ToolExecutionMode } from "@earendil-works/pi-agent-core";
 import type { Api, AssistantMessageEvent, AssistantMessageEventStream, ConstrainedSamplingConfig, Context, ImageContent, Model, OAuthCredentials, OAuthLoginCallbacks, Provider, ProviderHeaders, RefreshModelsContext, SimpleStreamOptions, TextContent, ToolResultMessage, Usage } from "@earendil-works/pi-ai";
-import type { AutocompleteItem, AutocompleteProvider, Component, EditorComponent, EditorTheme, KeyId, OverlayHandle, OverlayOptions, TUI } from "@earendil-works/pi-tui";
+import type { AutocompleteItem, AutocompleteProvider, Component, ComponentBounds, EditorComponent, EditorTheme, KeyId, OverlayHandle, OverlayOptions, TUI } from "@earendil-works/pi-tui";
 import type { Static, TSchema } from "typebox";
 import type { Theme } from "../../modes/interactive/theme/theme.ts";
 import type { BashResult } from "../bash-executor.ts";
@@ -310,6 +310,12 @@ export interface ToolRenderResultOptions {
     /** Whether this is a partial/streaming result */
     isPartial: boolean;
 }
+/** Viewport geometry owned by Pi for the current tool result renderer. */
+export interface ToolResultLayout {
+    readonly tui: TUI;
+    readonly bounds: ComponentBounds | undefined;
+    onChange(listener: (bounds: ComponentBounds | undefined) => void): () => void;
+}
 /** Context passed to tool renderers. */
 export interface ToolRenderContext<TState = any, TArgs = any> {
     /** Current tool call arguments. Shared across call/result renders for the same tool call. */
@@ -336,6 +342,8 @@ export interface ToolRenderContext<TState = any, TArgs = any> {
     showImages: boolean;
     /** Whether the current result is an error. */
     isError: boolean;
+    /** Pi-owned viewport geometry for this result slot, if it is currently laid out. */
+    resultLayout: ToolResultLayout;
 }
 /**
  * Tool definition for registerTool().
@@ -832,6 +840,36 @@ export interface MessageRenderOptions {
     /** Horizontal padding configured by the outputPad setting. */
     outputPad: number;
 }
+/** Pi-owned visible text and inner geometry for one assistant text block. */
+export interface AssistantMessageAddonSnapshot {
+    readonly bounds: ComponentBounds;
+    readonly lines: readonly string[];
+}
+/** Pi-owned layout stream for one visible assistant text block. */
+export interface AssistantMessageAddonLayout {
+    readonly snapshot: AssistantMessageAddonSnapshot | undefined;
+    onChange(listener: (snapshot: AssistantMessageAddonSnapshot | undefined) => void): () => void;
+}
+/** A visible text or thinking component inside a builtin assistant message. */
+export interface AssistantMessageAddonBlock {
+    readonly kind: "text" | "thinking";
+    readonly layout: AssistantMessageAddonLayout;
+}
+/** Host context passed to an assistant-message addon factory. */
+export interface AssistantMessageAddonContext {
+    readonly tui: TUI;
+    readonly component: Component;
+    readonly outputPad: number;
+    readonly theme: Theme;
+}
+/** Addon-owned behavior layered onto Pi's existing assistant renderer. */
+export interface AssistantMessageAddon {
+    setBlocks(blocks: readonly AssistantMessageAddonBlock[]): void;
+    setOutputPad(padding: number): void;
+    render(lines: readonly string[], width: number): string[];
+    dispose(): void;
+}
+export type AssistantMessageAddonFactory = (context: AssistantMessageAddonContext) => AssistantMessageAddon | undefined;
 export interface EntryRenderOptions {
     expanded: boolean;
 }
@@ -905,6 +943,8 @@ export interface ExtensionAPI {
     getFlag(name: string): boolean | string | undefined;
     /** Register a custom renderer for CustomMessageEntry. */
     registerMessageRenderer<T = unknown>(customType: string, renderer: MessageRenderer<T>): void;
+    /** Register an addon layered onto Pi's builtin assistant-message renderer. */
+    registerAssistantMessageAddon(factory: AssistantMessageAddonFactory): void;
     /** Register a custom renderer for CustomEntry. Custom entries do not participate in LLM context. */
     registerEntryRenderer<T = unknown>(customType: string, renderer: EntryRenderer<T>): void;
     /** Send a custom message to the session. */
@@ -1248,6 +1288,7 @@ export interface Extension {
     handlers: Map<string, HandlerFn[]>;
     tools: Map<string, RegisteredTool>;
     messageRenderers: Map<string, MessageRenderer>;
+    assistantMessageAddon?: AssistantMessageAddonFactory;
     entryRenderers?: Map<string, EntryRenderer>;
     commands: Map<string, RegisteredCommand>;
     flags: Map<string, ExtensionFlag>;
diff --git a/dist/index.d.ts b/dist/index.d.ts
index e91e38f8bedfabb6e67305f9739277d1ebacde22..b0d3163848531bdc5633c2b6d60b78a54eeb9600 100644
--- a/dist/index.d.ts
+++ b/dist/index.d.ts
@@ -4,6 +4,8 @@ export { AgentSession, type AgentSessionConfig, type AgentSessionEvent, type Age
 export { readStoredCredential } from "./core/auth-storage.ts";
 export { type BranchPreparation, type BranchSummaryResult, type CollectEntriesResult, type CompactionResult, type CutPointResult, calculateContextTokens, collectEntriesForBranchSummary, compact, DEFAULT_COMPACTION_SETTINGS, estimateTokens, type FileOperations, findCutPoint, findTurnStartIndex, type GenerateBranchSummaryOptions, generateBranchSummary, generateSummary, generateSummaryWithUsage, getLastAssistantUsage, prepareBranchEntries, serializeConversation, shouldCompact, } from "./core/compaction/index.ts";
 export { createEventBus, type EventBus, type EventBusController } from "./core/event-bus.ts";
+export type { AssistantMessageAddon, AssistantMessageAddonBlock, AssistantMessageAddonContext, AssistantMessageAddonFactory, AssistantMessageAddonLayout, AssistantMessageAddonSnapshot } from "./core/extensions/types.ts";
+export type { ToolRenderContext, ToolResultLayout } from "./core/extensions/types.ts";
 export type { AgentEndEvent, AgentSettledEvent, AgentStartEvent, AgentToolResult, AgentToolUpdateCallback, AppKeybinding, AutocompleteProviderFactory, BashToolCallEvent, BeforeAgentStartEvent, BeforeAgentStartEventResult, BeforeProviderHeadersEvent, BeforeProviderRequestEvent, BeforeProviderRequestEventResult, BuildSystemPromptOptions, CompactOptions, ContextEvent, ContextUsage, CustomToolCallEvent, EditToolCallEvent, EntryRenderer, EntryRenderOptions, ExecOptions, ExecResult, Extension, ExtensionActions, ExtensionAPI, ExtensionCommandContext, ExtensionCommandContextActions, ExtensionContext, ExtensionContextActions, ExtensionError, ExtensionEvent, ExtensionFactory, ExtensionFlag, ExtensionHandler, ExtensionRuntime, ExtensionShortcut, ExtensionUIContext, ExtensionUIDialogOptions, ExtensionWidgetOptions, FindToolCallEvent, GrepToolCallEvent, InlineExtension, InputEvent, InputEventResult, InputSource, KeybindingsManager, LoadExtensionsResult, LsToolCallEvent, MessageEndEvent, MessageRenderer, MessageRenderOptions, MessageStartEvent, MessageUpdateEvent, ProjectTrustContext, ProjectTrustEvent, ProjectTrustEventDecision, ProjectTrustEventResult, ProjectTrustHandler, ProviderConfig, ProviderModelConfig, ReadToolCallEvent, RegisteredCommand, RegisteredTool, ResolvedCommand, SessionBeforeCompactEvent, SessionBeforeForkEvent, SessionBeforeSwitchEvent, SessionBeforeTreeEvent, SessionCompactEvent, SessionInfoChangedEvent, SessionShutdownEvent, SessionStartEvent, SessionTreeEvent, SlashCommandInfo, SlashCommandSource, SourceInfo, TerminalInputHandler, ToolCallEvent, ToolCallEventResult, ToolDefinition, ToolExecutionEndEvent, ToolExecutionMode, ToolExecutionStartEvent, ToolExecutionUpdateEvent, ToolInfo, ToolRenderResultOptions, ToolResultEvent, TurnEndEvent, TurnStartEvent, UserBashEvent, UserBashEventResult, WidgetPlacement, WorkingIndicatorOptions, WriteToolCallEvent, } from "./core/extensions/index.ts";
 export { createExtensionRuntime, defineTool, discoverAndLoadExtensions, ExtensionRunner, isBashToolResult, isEditToolResult, isFindToolResult, isGrepToolResult, isLsToolResult, isReadToolResult, isToolCallEventType, isWriteToolResult, wrapRegisteredTool, wrapRegisteredTools, } from "./core/extensions/index.ts";
 export type { ReadonlyFooterDataProvider } from "./core/footer-data-provider.ts";
diff --git a/dist/modes/interactive/components/assistant-message.d.ts b/dist/modes/interactive/components/assistant-message.d.ts
index 8856a5f22046870774a86cbe097b5b025dcdeca3..4239a59cbaa1022456e80961b67ea4a7c110def4 100644
--- a/dist/modes/interactive/components/assistant-message.d.ts
+++ b/dist/modes/interactive/components/assistant-message.d.ts
@@ -1,5 +1,6 @@
 import type { AssistantMessage } from "@earendil-works/pi-ai";
-import { Container, type MarkdownTheme } from "@earendil-works/pi-tui";
+import { Container, type MarkdownTheme, type TUI } from "@earendil-works/pi-tui";
+import type { AssistantMessageAddonFactory } from "../../../core/extensions/types.ts";
 /**
  * Component that renders a complete assistant message
  */
@@ -11,11 +12,12 @@ export declare class AssistantMessageComponent extends Container {
     private outputPad;
     private lastMessage?;
     private hasToolCalls;
-    constructor(message?: AssistantMessage, hideThinkingBlock?: boolean, markdownTheme?: MarkdownTheme, hiddenThinkingLabel?: string, outputPad?: number);
+    constructor(message?: AssistantMessage, hideThinkingBlock?: boolean, markdownTheme?: MarkdownTheme, hiddenThinkingLabel?: string, outputPad?: number, tui?: TUI, addonFactory?: AssistantMessageAddonFactory);
     invalidate(): void;
     setHideThinkingBlock(hide: boolean): void;
     setHiddenThinkingLabel(label: string): void;
     setOutputPad(padding: number): void;
+    dispose(): void;
     render(width: number): string[];
     updateContent(message: AssistantMessage): void;
 }
diff --git a/dist/modes/interactive/components/assistant-message.js b/dist/modes/interactive/components/assistant-message.js
index 2ec54e135dbb2fe0283101c5e30b7ef69d0d665e..b15a108e19a1a5d6a5ea8756fb2e69d241aac560 100644
--- a/dist/modes/interactive/components/assistant-message.js
+++ b/dist/modes/interactive/components/assistant-message.js
@@ -1,4 +1,4 @@
-import { Container, Markdown, Spacer, Text } from "@earendil-works/pi-tui";
+import { Container, Markdown, sliceByColumn, Spacer, Text } from "@earendil-works/pi-tui";
 import { getMarkdownTheme, theme } from "../theme/theme.js";
 const OSC133_ZONE_START = "\x1b]133;A\x07";
 const OSC133_ZONE_END = "\x1b]133;B\x07";
@@ -14,15 +14,27 @@ export class AssistantMessageComponent extends Container {
     outputPad;
     lastMessage;
     hasToolCalls = false;
-    constructor(message, hideThinkingBlock = false, markdownTheme = getMarkdownTheme(), hiddenThinkingLabel = "Thinking...", outputPad = 1) {
+    assistantAddon;
+    tui;
+    constructor(message, hideThinkingBlock = false, markdownTheme = getMarkdownTheme(), hiddenThinkingLabel = "Thinking...", outputPad = 1, tui, addonFactory) {
         super();
         this.hideThinkingBlock = hideThinkingBlock;
         this.markdownTheme = markdownTheme;
         this.hiddenThinkingLabel = hiddenThinkingLabel;
         this.outputPad = outputPad;
+        this.tui = tui;
         // Container for text/thinking content
         this.contentContainer = new Container();
         this.addChild(this.contentContainer);
+        if (tui && addonFactory) {
+            try {
+                this.assistantAddon = addonFactory({ tui, component: this, outputPad, theme });
+            }
+            catch {
+                // Extension addons are optional; preserve Pi's renderer when one fails to initialize.
+                this.assistantAddon = undefined;
+            }
+        }
         if (message) {
             this.updateContent(message);
         }
@@ -47,12 +59,24 @@ export class AssistantMessageComponent extends Container {
     }
     setOutputPad(padding) {
         this.outputPad = padding;
+        try {
+            this.assistantAddon?.setOutputPad(padding);
+        }
+        catch {
+            // Optional addon failure must not block Pi's setting update.
+        }
         if (this.lastMessage) {
             this.updateContent(this.lastMessage);
         }
     }
     render(width) {
-        const lines = super.render(width);
+        let lines = super.render(width);
+        try {
+            lines = this.assistantAddon?.render(lines, width) ?? lines;
+        }
+        catch {
+            // A render addon must not make builtin assistant output unavailable.
+        }
         if (this.hasToolCalls || lines.length === 0) {
             return lines;
         }
@@ -64,6 +88,7 @@ export class AssistantMessageComponent extends Container {
         this.lastMessage = message;
         // Clear content container
         this.contentContainer.clear();
+        const addonBlocks = [];
         const hasVisibleContent = message.content.some((c) => (c.type === "text" && c.text.trim()) || (c.type === "thinking" && c.thinking.trim()));
         if (hasVisibleContent) {
             this.contentContainer.addChild(new Spacer(1));
@@ -74,7 +99,9 @@ export class AssistantMessageComponent extends Container {
             if (content.type === "text" && content.text.trim()) {
                 // Assistant text messages with no background - trim the text
                 // Set paddingY=0 to avoid extra spacing before tool executions
-                this.contentContainer.addChild(new Markdown(content.text.trim(), this.outputPad, 0, this.markdownTheme));
+                const block = new Markdown(content.text.trim(), this.outputPad, 0, this.markdownTheme);
+                this.contentContainer.addChild(block);
+                addonBlocks.push({ kind: "text", layout: this.getAddonLayout(block) });
             }
             else if (content.type === "thinking") {
                 const thinkingBlocks = [];
@@ -103,10 +130,12 @@ export class AssistantMessageComponent extends Container {
                 }
                 else {
                     // Render each run of thinking blocks as one Markdown section.
-                    this.contentContainer.addChild(new Markdown(thinkingBlocks.join("\n\n"), this.outputPad, 0, this.markdownTheme, {
+                    const block = new Markdown(thinkingBlocks.join("\n\n"), this.outputPad, 0, this.markdownTheme, {
                         color: (text) => theme.fg("thinkingText", text),
                         italic: true,
-                    }));
+                    });
+                    this.contentContainer.addChild(block);
+                    addonBlocks.push({ kind: "thinking", layout: this.getAddonLayout(block) });
                 }
                 if (hasVisibleContentAfter) {
                     this.contentContainer.addChild(new Spacer(1));
@@ -133,9 +162,46 @@ export class AssistantMessageComponent extends Container {
             else if (message.stopReason === "error") {
                 const errorMsg = message.errorMessage || "Unknown error";
                 this.contentContainer.addChild(new Spacer(1));
-                this.contentContainer.addChild(new Text(theme.fg("error", `Error: ${errorMsg}`), this.outputPad, 0));
+                this.contentContainer.addChild(new Text(theme.fg("error", errorMsg), this.outputPad, 0));
             }
         }
+        try {
+            this.assistantAddon?.setBlocks(addonBlocks);
+        }
+        catch {
+            // Block observation is optional and must not alter builtin assistant rendering.
+        }
+    }
+    dispose() {
+        try {
+            this.assistantAddon?.dispose();
+        }
+        catch {
+            // Optional addon cleanup must not interrupt Pi's chat teardown.
+        }
+        finally {
+            this.assistantAddon = undefined;
+        }
+    }
+    getAddonLayout(component) {
+        const tui = this.tui;
+        const snapshot = (bounds) => {
+            if (!bounds)
+                return undefined;
+            return {
+                bounds: {
+                    x: bounds.x + this.outputPad,
+                    y: bounds.y,
+                    width: Math.max(0, bounds.width - this.outputPad * 2),
+                    height: bounds.height,
+                },
+                lines: component.render(bounds.width).map((line) => sliceByColumn(line, this.outputPad, Math.max(0, bounds.width - this.outputPad * 2))),
+            };
+        };
+        return {
+            snapshot: snapshot(tui?.getComponentBounds(component)),
+            onChange: (listener) => tui ? tui.onComponentLayout(component, (bounds) => listener(snapshot(bounds))) : () => { },
+        };
     }
 }
 //# sourceMappingURL=assistant-message.js.map
\ No newline at end of file
diff --git a/dist/modes/interactive/components/tool-execution.d.ts b/dist/modes/interactive/components/tool-execution.d.ts
index 4a22d5c5159d72c18f4be5b607572238fdedd7cb..c1020257ddcc0197509f83c72daa03b318de20c2 100644
--- a/dist/modes/interactive/components/tool-execution.d.ts
+++ b/dist/modes/interactive/components/tool-execution.d.ts
@@ -35,6 +35,7 @@ export declare class ToolExecutionComponent extends Container {
     private hasRendererDefinition;
     private getRenderShell;
     private getRenderContext;
+    dispose(): void;
     private createCallFallback;
     private createResultFallback;
     updateArgs(args: any): void;
diff --git a/dist/modes/interactive/components/tool-execution.js b/dist/modes/interactive/components/tool-execution.js
index 3f93cc6c5665f603d29b3d6ac3dabfa560d6bb32..5d3b3a9335819ffc03a6e57be51193678c8210a2 100644
--- a/dist/modes/interactive/components/tool-execution.js
+++ b/dist/modes/interactive/components/tool-execution.js
@@ -28,6 +28,8 @@ export class ToolExecutionComponent extends Container {
     result;
     convertedImages = new Map();
     hideComponent = false;
+    resultLayoutListeners = new Set();
+    removeComponentLayoutListener;
     constructor(toolName, toolCallId, args, options = {}, toolDefinition, ui, cwd) {
         super();
         this.toolName = toolName;
@@ -39,6 +41,7 @@ export class ToolExecutionComponent extends Container {
         this.imageWidthCells = options.imageWidthCells ?? 60;
         this.ui = ui;
         this.cwd = cwd;
+        this.removeComponentLayoutListener = ui.onComponentLayout(this, () => this.publishResultLayout());
         this.addChild(new Spacer(1));
         // Always create all shell variants. contentBox is used for default renderer-based composition.
         // selfRenderContainer is used when the tool renders its own framing.
@@ -101,8 +104,40 @@ export class ToolExecutionComponent extends Container {
             expanded: this.expanded,
             showImages: this.showImages,
             isError: this.result?.isError ?? false,
+            resultLayout: {
+                tui: this.ui,
+                bounds: this.getResultBounds(),
+                onChange: (listener) => {
+                    this.resultLayoutListeners.add(listener);
+                    listener(this.getResultBounds());
+                    return () => this.resultLayoutListeners.delete(listener);
+                },
+            },
         };
     }
+    getResultBounds() {
+        const row = this.ui.getComponentBounds(this);
+        const result = this.resultRendererComponent;
+        if (!row || !result)
+            return undefined;
+        const self = this.hasRendererDefinition() && this.getRenderShell() === "self";
+        const x = self ? row.x : row.x + 1;
+        const width = Math.max(0, row.width - (self ? 0 : 2));
+        const callHeight = this.callRendererComponent?.render(width).length ?? 0;
+        const y = row.y + (self ? 1 : 2) + callHeight;
+        return { x, y, width, height: result.render(width).length };
+    }
+    publishResultLayout() {
+        const bounds = this.getResultBounds();
+        for (const listener of this.resultLayoutListeners)
+            listener(bounds);
+    }
+    dispose() {
+        this.removeComponentLayoutListener?.();
+        this.removeComponentLayoutListener = undefined;
+        this.resultLayoutListeners.clear();
+        this.resultRendererComponent?.dispose?.();
+    }
     createCallFallback() {
         return new Text(theme.fg("toolTitle", theme.bold(this.toolName)), 0, 0);
     }
diff --git a/dist/modes/interactive/interactive-mode.js b/dist/modes/interactive/interactive-mode.js
index 462e240c591ec86e57cf5d03ce41110d2e484fc4..c1ec4010f4bb8bd735d5524878d263d201bd0507 100644
--- a/dist/modes/interactive/interactive-mode.js
+++ b/dist/modes/interactive/interactive-mode.js
@@ -1255,6 +1255,7 @@ export class InteractiveMode {
                     if (result.cancelled) {
                         return { cancelled: true };
                     }
+                    this.disposeChatComponents();
                     this.chatContainer.clear();
                     this.renderInitialMessages();
                     if (result.editorText && !this.editor.getText().trim()) {
@@ -1339,6 +1340,7 @@ export class InteractiveMode {
     }
     renderCurrentSessionState() {
         this.loadedResourcesContainer.clear();
+        this.disposeChatComponents();
         this.chatContainer.clear();
         this.pendingMessagesContainer.clear();
         this.compactionQueuedMessages = [];
@@ -2345,7 +2347,7 @@ export class InteractiveMode {
                     this.ui.requestRender();
                 }
                 else if (event.message.role === "assistant") {
-                    this.streamingComponent = new AssistantMessageComponent(undefined, this.hideThinkingBlock, this.getMarkdownThemeWithSettings(), this.hiddenThinkingLabel, this.outputPad);
+                    this.streamingComponent = new AssistantMessageComponent(undefined, this.hideThinkingBlock, this.getMarkdownThemeWithSettings(), this.hiddenThinkingLabel, this.outputPad, this.ui, this.session.extensionRunner.getAssistantMessageAddonFactory());
                     this.streamingMessage = event.message;
                     this.chatContainer.addChild(this.streamingComponent);
                     this.streamingComponent.updateContent(this.streamingMessage);
@@ -2500,6 +2502,7 @@ export class InteractiveMode {
                     }
                 }
                 else if (event.result) {
+                    this.disposeChatComponents();
                     this.chatContainer.clear();
                     this.rebuildChatFromMessages();
                     this.addMessageToChat(createCompactionSummaryMessage(event.result.summary, event.result.tokensBefore, new Date().toISOString()));
@@ -2681,7 +2684,7 @@ export class InteractiveMode {
                 break;
             }
             case "assistant": {
-                const assistantComponent = new AssistantMessageComponent(message, this.hideThinkingBlock, this.getMarkdownThemeWithSettings(), this.hiddenThinkingLabel, this.outputPad);
+                const assistantComponent = new AssistantMessageComponent(message, this.hideThinkingBlock, this.getMarkdownThemeWithSettings(), this.hiddenThinkingLabel, this.outputPad, this.ui, this.session.extensionRunner.getAssistantMessageAddonFactory());
                 this.chatContainer.addChild(assistantComponent);
                 break;
             }
@@ -2847,10 +2850,17 @@ export class InteractiveMode {
             };
         });
     }
-    rebuildChatFromMessages() {
+    rebuildChatFromMessages(exclude) {
+        this.disposeChatComponents(exclude);
         this.chatContainer.clear();
         this.renderSessionEntries(this.sessionManager.buildContextEntries());
     }
+    disposeChatComponents(exclude) {
+        for (const child of this.chatContainer.children) {
+            if (child !== exclude && (child instanceof ToolExecutionComponent || child instanceof AssistantMessageComponent))
+                child.dispose();
+        }
+    }
     // =========================================================================
     // Key handlers
     // =========================================================================
@@ -3133,8 +3143,7 @@ export class InteractiveMode {
         this.hideThinkingBlock = !this.hideThinkingBlock;
         this.settingsManager.setHideThinkingBlock(this.hideThinkingBlock);
         // Rebuild chat from session messages
-        this.chatContainer.clear();
-        this.rebuildChatFromMessages();
+        this.rebuildChatFromMessages(this.streamingComponent);
         // If streaming, re-add the streaming component with updated visibility and re-render
         if (this.streamingComponent && this.streamingMessage) {
             this.streamingComponent.setHideThinkingBlock(this.hideThinkingBlock);
@@ -3493,6 +3502,7 @@ export class InteractiveMode {
                             child.setHideThinkingBlock(hidden);
                         }
                     }
+                    this.disposeChatComponents();
                     this.chatContainer.clear();
                     this.rebuildChatFromMessages();
                 },
@@ -3920,6 +3930,7 @@ export class InteractiveMode {
                         return;
                     }
                     // Update UI
+                    this.disposeChatComponents();
                     this.chatContainer.clear();
                     this.renderInitialMessages();
                     if (result.editorText && !this.editor.getText().trim()) {
@@ -5056,6 +5067,7 @@ export class InteractiveMode {
         this.clearStatusIndicator();
         this.themeController.disableAutoSync();
         this.clearExtensionTerminalInputListeners();
+        this.disposeChatComponents();
         this.footer.dispose();
         this.footerDataProvider.dispose();
         if (this.unsubscribe) {
